# 变更记录

[English](CHANGELOG.md) · 简体中文

RigorBreeze 的重要变更都会记录在本文档中。

项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。Public Preview 期间，如果真实项目证据表明现有合同不安全或带来不必要成本，次版本可能调整工作流接口。

## [Unreleased]

### 变更

- RigorBreeze 维护者任务合同、evidence 和 archive 改为仅保存在源码工作区本地；公开发布证明由提交、变更记录、回归测试和 CI 承担，不改变已初始化业务项目的证据行为。
- 在首次公开发布前，将项目、Skill ID、调用名、执行器、配置、Git 私有状态和任务分支前缀统一从 Codex Production Flow 更名为 RigorBreeze，不保留旧名兼容入口。
- 新增自定义代码或包前，必须先检查标准库、框架和当前依赖集；新依赖与抽象必须由当前验收或长期不变量证明。
- 将兼容性改为风险自适应规则：可删除已证明死亡且没有兼容承诺的预览/内部路径；公开 API、持久化数据、升级路径和生产迁移继续要求明确过渡、验证和回滚。

### 修复

- 当前 worktree 新建任务时按配置的顺序集成分支及其 Git SHA 冻结基线；配置基线不存在时在写入任务记录前失败。
- worktree 所有权检查在 Windows 上改用无副作用的原生进程查询，不再使用仅适合 POSIX 的 `os.kill(pid, 0)` 探测。
- 重复观察同一验收 ID 的 RED 时继续保留历史审计记录，但关闭门禁只评估最新的当前证据链。
- 在流支持重新配置时将 CLI 的 stdout 和 stderr 统一为 UTF-8，确保双语提示能在 Windows 旧代码页环境下正常输出。

### 文档

- 使用双语引导、明确的安全边界和完整首次任务示例，重新组织面向首次使用者的仓库文档。
- 增加精简的贡献指南、安全策略和 MIT 许可证。
- 为全部用户文档和 Skill 参考文档建立中文版本及一致性检查。

## [0.15.0] - 在不扩张工作流表面的前提下收紧 Agent 行为

### 变更

- 合同与验收结果未变化时，在不可变批准基线上重新观察补强后的测试；生产文件覆盖和无关环境失败不能冒充 RED。
- `status --all` 与 cleanup 按物理 worktree 聚合，保留兼容代表任务 ID，并分别展示实际执行的 bundled Runner 和项目安装 Runner。
- 下一动作显式携带 `actor` 与 `kind`，确保“执行已批准切片”仍由 Codex 继续，而不会因文案关键词误交给用户批准。
- 扩大范围前，Agent 必须先证明问题属于错误/缺失数据、错误配置，还是缺少能力，并把最小修正与可选预防拆开。
- 尚未被区分性运行证据证明的根因一律按“假设”处理，不能把评审猜测或重复失败直接当成确认诊断。
- 指派人工验收前，必须核实真实 route、menu、role、account、device 入口；若某个入口不可用，应诚实报告 `N/A`，并改用等价运行/API 证据。
- 登录、token、浏览器或渠道连续两次同样失败后，必须停止沿同一路径重试，先保留安全状态，再切换诊断方法。
- 三个或更多相关页面，或新的视觉语言，在大范围铺开前必须先做一个 visual tracer；只有精确复用已批准组件时才可跳过。

### 验证

- 确定性 Agent 行为合同从十四个扩展为十六个场景：扩展现有 context/diagnosis 场景，并新增 runtime-affordance-before-handoff 与 visual-tracer-before-fanout。
- 行为评分以真实命令、修改路径、新鲜验证和必填的用户结果摘要为准，不再信任 Agent 自报标签；读取到的规则原文和命令输出不能冒充 Agent 已执行的动作。

### 兼容性

- 公共 CLI、Spec Tree、状态/配置 schema v5、evidence schema v4、automation journal v1 和依赖均不改变。bundled Runner 升级到 v0.15.0；项目存在活动任务时仍冻结已安装 Runner，关闭后再更新。

## [0.14.0] - 让严谨程度匹配真实后果

### 新增

- 增加无任务 Direct 通道：一个无歧义低后果修改只需一项定向检查。
- 增加 schema v5 Git 公共私有 records、显式旧记录迁移、L1 精简历史和受限脱敏 L2/Emergency 审计摘要。
- 增加“已完成/当前/唯一下一动作”交互投影，以及干净 CI checkout 的无状态 enforced full 检查。

### 变更

- 分离分配依据：风险决定门禁，Direct 之外的独立结果使用短期分支，只有并发写窗口或可丢弃的高风险实验才增加 worktree；顺序独立任务复用物理目录但不沿用前任任务分支。
- 清晰 L1 请求直接作为批准来源，干净首次通过的 L1 自动关闭；有摩擦的 L1 和全部 L2/Emergency 保留人工判断。
- 自动诚实收口已集成任务，只安全删除已包含的受管 worktree 和无远端不确定性的本地分支。
- 完整 evidence 继续使用 schema v4，automation journal 继续 v1；没有增加公共命令、Spec 文件类型、依赖或降低生产门禁。

## [0.13.0] - 将可复用流程修复与业务交付隔离

### 修复

- 破坏性迁移策略只检查活动任务已提交和工作树中的变化：没有改动的历史 SQL 不再阻塞无关任务，当前任务新改的破坏性迁移仍然失败。

### 变更

- 业务任务暴露可复用 RigorBreeze 缺陷时，保留业务合同并记录阻塞或演进候选，再创建独立 Skill 任务修复；不得扩大产品任务或修改其私有 Runner。
- 顺序执行的项目切片复用指定 integration worktree；只有真正并发的写任务或可丢弃的高风险实验才新建 worktree。

### 验证

- 确定性 Agent 行为合同从十一个扩展为十三个场景，新增业务任务/流程缺陷隔离和顺序项目 worktree 复用场景。

### 兼容性

- 公共 CLI、Spec Tree、state/evidence schema v4、automation journal v1、依赖、默认 Git 权限和普通 L0/L1 交互均不改变。

## [0.12.0] - 提升 Agent 决策质量而不增加流程重量

### 变更

- 新项目塑形和真正存在分支结果的 L2 歧义采用决策前沿：先恢复事实，每轮最多询问三个当前可回答且会改变结果的问题，并提供推荐答案、理由和结果影响。
- 把原型限定为一件回答一个决策问题的可丢弃产物；保留引用、观察结果和结论，但不得把它当成生产实现或真实验收。
- 保留 helper、adapter、wrapper、配置层或共享抽象前执行删除测试；复杂度随删除消失的层应移除，只有能够集中真实调用方或安全复杂度的边界才保留。
- 新增始终加载的 Skill 指令必须说明触发条件、完成条件和行为测试变化；没有可观察效果的文字应删除或下沉到现有 reference。
- 区分稳定发布安装和贡献者软链接；只有活动任务关闭且状态显示 `outdated` 与 `upgradeSafe=true` 时才升级项目 Runner。

### 验证

- 确定性行为合同从九个扩展为十一个场景，新增决策前沿和单问题原型，并在评审怀疑场景加入删除测试。
- 发布候选需要每个场景手动运行两次真实 Codex；普通提交、配置 profile 和 CI 仍不会调用模型。

### 兼容性

- 公共 CLI、Spec Tree、state/evidence schema v4、automation journal v1、依赖、默认 Git 权限和普通 L0/L1 交互均不改变。

## [0.11.0] - 普通个人开发保持轻量，生产门禁不降级

### 变更

- 查询状态、解释日志、分析截图、给建议和其他不写代码的诊断走无任务通道；过期工作流状态会被报告，但不能阻塞只读回答。
- L0/L1/L2 按后果而非 diff 大小或紧急程度选择；真正隔离的 L0 若准备成本高于实现成本，应删除无关仪式。
- 远程写入前冻结已批准发布范围；关键发现停止发布，无关的镜像、操作系统、数据库、扫描器、框架或平台升级建立独立治理任务。
- 跨任务交接必须可见，明确目标任务、结果、允许与禁止范围、依赖或阻塞项和负责人。

### 修复

- 正常完成任务的 TDD 历史按验收 ID 压缩为最终有效 GREEN 与最近一次有价值的失败或失效链，并保留汇总次数；活动、abandoned 和 reconciled 历史保持完整。
- 可分发 Skill ZIP 排除维护者测试、字节码和缓存，但源码仓库继续保留回归测试。

### 兼容性

- 公共 CLI 命令、state/evidence schema v4、automation journal v1、Spec Tree、依赖、Git 权限与项目适配器边界均不改变。
- 精简的 tracked 任务/evidence 继续作为审计模型；明确暂缓建立第二套本地 evidence 仓库。

## [0.10.4] - 高风险工作前恢复过期流程记录

### 修复

- 活动合同缺失时统一 `status` 与 `doctor`：投影 `orphaned-record`、禁止推断完成，并给出非破坏性恢复动作，不再出现一个命令崩溃而另一个显示健康。
- status 直接提示已有演进候选和一条可复制的汇总指令，不复制或修改任务 evidence。
- 在创建新的 Git L1/L2 任务前检查已安装 Runner 与真实基准分支工作流基线；L0 和已有任务恢复仍保持轻量。
- 为有意预建的草稿保留精简 `Task-Origin` 与 `Waiting-On`，避免因为尚无实现就被误判为可以废弃。
- 新增第七个仓库级 Agent 压力场景，拒绝在高风险工作流损坏后改用非正式手工任务卡继续写入。

### 兼容性

- 公共 CLI 命令、state/evidence schema v4、automation journal v1、Spec Tree、依赖、Git 权限与项目适配器边界均不改变。

## [0.10.3] - 已完成验证证据压缩

### 变更

- 只在任务正常完成归档时压缩重复 `checkRuns`：每个 profile/check 保留最新记录和最近一次历史失败，并保存通过、失败和省略数量汇总。
- 回归测试、profile 级验证历史、RED/GREEN、验收、制品、实践、关闭事实以及 abandoned/reconciled 历史全部保持不变。
- 普通审查优先写入结构化 evidence，不再默认创建第二份任务 Markdown；临时完整日志放入忽略目录或有期限的 CI Artifact。

### 兼容性

- 公共 CLI、schema v4、automation journal v1、Spec Tree、依赖、Git 权限和归档前门禁行为均不改变；没有 `checkRunSummary` 的现有证据继续有效。

## [0.10.2] - 共享 worktree 修复一致性

### 修复

- Git 确认删除精确匹配的受管 worktree 后，同步把所有引用该路径的注册表记录标记为已移除，避免后续投影进入已经删除的目录。
- 同一主工作区中的归档记录与当前活动任务按串行历史处理，不再误报为重复活动所有权。
- 原 worktree 已删除并通过归档证据重建的历史任务不再与当前主分支做错误分支比较；两个活动任务共享 worktree 仍然阻断。

### 兼容性

- 公共 CLI、schema v4、automation journal v1、Spec Tree、依赖、分支保留策略和 Git 权限均不改变。

## [0.10.1] - 历史状态无损修复

### 修复

- `integrated-unclosed` 状态与 `archive --outcome reconciled` 统一使用已登记集成证明，确保只剩白名单工作流提交时，建议的安全收口动作能够真实执行。
- 从全部归档合同和证据重建 Git 私有注册表，保留历史上合法共享同一 worktree 的多条已关闭任务。
- 允许多个已关闭任务引用同一保留 worktree，同时继续阻断重复的活动写入所有权。
- JSON doctor 阻断时先输出机器可读修复计划，再由用户显式执行 `doctor --all --repair --json` 修改可重建索引。

### 兼容性

- 公共 CLI、schema v4、automation journal v1、Spec Tree、依赖和 Git 权限均不改变。

## [0.10.0] - 项目塑形与生命周期真实性

### 新增

- 为新产品、新业务域、大范围旧系统迁移或尚无法稳定为一份交付合同的想法增加按需项目塑形；先比较可行方案并收口产品风险歧义，再只把首个纵向切片接入现有最小 Spec Tree。
- 活动任务尚未批准却已出现交付改动时，在当前与全 worktree 状态中投影 `workflowBypass`，并持久化一条去重的即时演进候选机器事件。

### 修复

- 已登记任务只要至少一个产品补丁已经等价进入基准分支，且剩余正向提交全部属于严格白名单工作流元数据，即可诚实识别为 `integrated-unclosed`。
- 混合或未匹配产品改动仍保持活动，未登记 worktree 继续采用原有保守证明。

### 兼容性

- 公共 CLI、state/evidence schema v4、automation journal v1、Spec Tree、运行时依赖和 Git 权限均不改变。

## [0.9.2] - 需求理解可靠性

### 变更

- 批准前将复合需求拆成可观察的 `ADD`、`REMOVE`、`MOVE`、`RETAIN`、`REPLACE` 原子，每项映射到验收 ID 或明确的不做原因。
- 根据项目证据判断否定表达是在描述当前问题还是目标结果；只有方向仍存在会改变结果的歧义时才问一个简短问题。
- UI 最终状态必须覆盖存在、不存在、位置/顺序和保留行为，不新增文档、字段、命令或用户表单。
- 用户纠正理解时，通过现有复盘证据归类为漏项、意图反转、事实源错误或范围变化，不保存聊天全文。

### 测试

- 用复合 UI 场景替换原有语义压力 fixture，拒绝遗漏移动/删除/保留操作或错误解释否定方向的部分需求，同时维持六场景结构。
- 真实行为评估只额外允许写入生成 fixture 的 Git 私有工作流状态，并把结构化结果绑定到准确场景 ID，避免将正确 Agent 行为误报为测试器失败。
- 将真实评估中的“部分交付”明确限定为遗漏用户可见需求原子，避免把可选工作流尚未收口误报为产品仅部分实现。

### 兼容性

- 公共 CLI、state/evidence schema v4、automation journal v1、Spec Tree、运行时依赖和 Git 权限均不改变。

## [0.9.1] - 低摩擦验证收口

### 变更

- 仅在单次 profile 调用内复用完全相同的 `argv + cwd + env + timeout` 进程结果，同时保留各检查独立的记录、报告和制品校验。
- 普通提交可以使用当前配置化 `affected` 或 `full` 证据；targeted 命令仍只用于探索，archive、merge 和直推集成分支继续执行 full 质量门禁。
- 核心执行器测试只运行一次并使用 240 秒超时，确定性行为合同测试作为独立 acceptance。真实 Codex 行为测试仍由维护者在发布候选阶段显式运行，commit、full 和 CI 都不会触发。

### 修复

- 将超时 stdout/stderr 的 bytes、文本或缺失值统一转换，稳定记录退出码 124、已有脱敏输出和明确超时原因。
- 只豁免配置测试路径下与 `rigorbreeze: synthetic-secret` 位于同一物理行的合成秘密；秘密路径、未标记行和项目配置的秘密扫描器继续强制执行。

### 兼容性

- 公共 CLI、state/evidence schema v4、automation journal v1、Spec Tree、依赖和交付权限均不改变。

## [0.9.0] - Agent 行为可靠性

### 新增

- 增加六个仅属于仓库的合成压力场景、标准库评分器和可选的 `codex exec --ephemeral --json` 维护者运行器。真实运行的脱敏结果只写入 Git 私有目录；CI 仅使用确定性伪造 transcript，绝不调用模型。
- 增加四条紧凑执行规则：批准前语义自检、完成声明绑定新鲜命令/退出状态/覆盖范围、评审意见先核验，以及同一缺陷三个假设失败后的架构停点。

### 修复

- 已关闭任务投影会清除过期复盘 `nextAction`，归档后不再建议继续执行旧动作。

### 兼容性

- 公共 CLI、state/evidence schema v4、automation journal v1、Spec Tree、风险等级、运行时依赖和普通项目操作均不改变。

## [0.8.1] - 证据驱动的上下文补全

### 变更

- 在现有任务合同中从权威需求、代码、测试、Git 和运行证据补全不完整提示词；只有无法恢复且会改变结果的意图才询问用户。
- 上下文压缩后的后续写任务，或同时使用调试/审查 Skill 时，必须重新进入 RigorBreeze 状态与认领检查。
- 外部 Git、部署、开发者工具和平台写操作前，先报告观察到的当前状态、已完成工作、不可变标识、剩余动作和停止条件。
- 配置在源码路径内部的测试目录不再被 RED 前门禁误判为生产代码变化。

### 兼容性

- CLI、state/evidence schema v4、automation journal v1、Spec 文件类型和运行时依赖均不变。

## [0.8.0] - 状态收口与低摩擦落地

### 新增

- `workflowBaseline` 在真正基准分支证明受管安装，并提供一条精确、由用户单次授权的基线提交路径。
- `archive --outcome reconciled` 可以诚实关闭已由外部 Git 操作集成的历史任务，不伪造验证或验收。
- 正常 archive 保存不可变 `lastClosed` 交付上下文，使受保护的 commit、push 和 merge 能在任务关闭后完成。
- 未登记 worktree 清理要求绝对路径、基准分支、精确 HEAD、干净无活动状态和完整集成证明，并始终保留分支。

### 变更

- 主 worktree 与 linked worktree 状态统一迁入 Git 私有目录；旧状态只在可证明安全时迁移和删除。
- 安装状态会区分缺失与被修改的 runner 组件；生命周期优先报告已集成未关闭和关闭待提交，不再被过期基线建议覆盖。
- enforced L1/L2 不再接受只提交在任务分支上的工作流基线。
- L2 指南优先采用脱敏真实/厂商沙箱 fixture，并明确验证序列化、编码、数据库方言和业务前置条件。

## [0.7.0] - 真实开发闭环

### 新增

- 项目安装状态会展示 bundled Skill 版本、项目执行器版本、漂移状态和是否可安全升级；活动实施任务会阻止静默替换执行器。
- `archive --outcome abandoned --reason ...` 可以安全关闭被取消或替代的任务，不伪造验证成功，也不删除分支、worktree 或提交。
- 任务通过 `Runtime-Claims` 声明独占端口、进程、服务、应用和环境，并在所有活动 worktree 之间机器阻断冲突。
- L2 条件化集成通过 `Operational-Modes` 将启用、关闭和依赖不可用行为绑定到验收 ID。
- 机器 JSON `operation-plan` 与 `operation-result` 发布证据记录有序阶段、停止条件、当前安全状态和唯一恢复入口。
- 执行器漂移、旧任务占位、运行资源冲突、缺少发布计划和门禁失败会去重写入任务自己的实践事件，并进入现有复盘。

### 变更

- state 与 evidence schema 从 v3 升至 v4，同时保留历史 RED、验证、验收、发布、自动化和实践记录。
- enforced L1/L2 批准要求执行器、配置、辅助模块和 Spec 索引已经进入 Git 基线。
- Skill 协议要求在写产品代码前通过 bundled runner 查询状态、获得已批准任务合同并成功认领当前窗口。

### 安全

- RigorBreeze 不杀进程、不抢端口、不覆盖活动任务执行器、不删除废弃任务的 Git 引用，也不把操作快照扩张成常驻部署引擎。

## [0.6.1] - 受控 Worktree 生命周期

### 变更

- 当任务从已记录基线开始的每个提交都能在基准分支找到补丁等价提交时，将任务识别为已集成，从而安全覆盖 cherry-pick，同时不会把部分集成误判为完成。
- 为 `status --all --json` 增加向后兼容的 `cleanup` 投影，区分可清理、需保留和未登记 worktree，以及默认保留的本地任务分支。
- 在人类可读的项目状态中显示清理数量，让 Codex 能主动协调已完成的受管 worktree，不依赖用户记忆。

### 安全

- 清理仍要求 RigorBreeze 创建来源、准确创建路径、干净且非当前的 worktree，以及已经证明集成。
- 已成功创建或集成的任务分支继续保留，生命周期清理不会删除本地或远程引用；只有 worktree 初始化本身失败时，原子回滚才可能删除刚创建的分支。

## [0.6.0] - 内核边界与按需交付

### 变更

- 将策略执行器拆成一个稳定 CLI 入口，以及 state、policy、parallel 和 automation 职责模块；CLI、schema v3 evidence 模型和 Spec Tree 不变。
- 将 `manual` 重新定义为“没有长期无人值守授权”。用户当前消息明确要求时，可以单次授权受保护的 commit 或 push，且不修改项目配置。
- 在现有 `automate` 命令中增加单次 commit/push 参数。单次 push 必须明确 remote、当前分支和准确的 expected HEAD，要求远端可 fast-forward，并在完成后核对 SHA；不会 rebase 或 force push。
- 单次直推集成分支必须具备当前 full 验证、结构化验收和审查。

### 修复

- 自动 commit 后，如果日志中的 parent、tree、任务、evidence、验证和项目指纹仍表示同一不可变结果，则原验证继续有效。
- 任务在物理基准分支上执行时，允许从已记录 base SHA 向前推进，同时仍由完整变化集和 Allowed Scope 阻断范围外变化。

### 兼容性

- evidence schema 继续为 v3，automation journal 继续为 v1；新日志仅增加向后兼容的 `authorizationMode` 投影。
- 未增加公共命令、Spec 文件类型、状态系统、运行时依赖或长期权限。

## [0.5.2] - 真实使用安全收口

### 变更

- Allowed Scope 强制使用仓库相对路径、目录前缀或 glob，验收条件强制使用唯一且机器可读的 ID。
- 禁止首次批准或重新批准把已修改生产实现吸收到新的 RED 基线。回退生产变化后可以修订同一结果；新增结果或验收条件才建立依赖切片。
- L1/L2 RED 必须引用真实测试文件和合同中已声明的验收 ID，并由当前 full 验证闭合全部有效 TDD 链。
- verify、merge、archive 和可选 Git 自动化都会检查已提交与未提交变化的范围漂移。
- 范围 glob 使用路径语义，Git 状态使用 NUL 安全解析，工作流策略文件要求显式范围，并覆盖全部已提交变化类型。
- L2 full 根据风险以及依赖、迁移实际变化推导必需检查。

### 修复

- 在加载内置模块前禁用 Python bytecode，首次命令不再向业务项目写入 `__pycache__`。
- L1/L2 尚未建立 Git 工作流基线时由 doctor 提示，但不自动提交。

### 兼容性

- evidence schema 继续为 v3；`status --json` 只增加向后兼容的 `scope` 投影。
- 历史 evidence 继续可读，但旧版本执行器产生的验证对于 v0.5.2 门禁视为过期。

## [0.5.1] - Public Preview 基线

### 新增

- 最小 SDD 任务合同，以及与机器绑定的 TDD、验证、验收、制品和复盘证据。
- 项目声明式 advisory/enforced 检查 profile，以及 GitHub/GitLab CI 模板。
- 隔离并行 worktree、可选依赖 DAG、范围冲突检测和跨窗口状态恢复。
- 默认手动的 commit、push、受保护 merge 和 release 适配器，以及 Git 私有幂等记录。
- 具备来源证明的安全 worktree 协调，以及 schema v1/v2 到 v3 的证据迁移。

### 成熟度

- 这是首个 Public Preview 基线，不代表已经达到 production-ready。
- v1.0 仍要求完成真实 L1/L2 切片、并行与 DAG 使用、远程 CI 和受保护交付演练。
