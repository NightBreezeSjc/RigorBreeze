# RigorBreeze：使用与能力演进

> 维护者实践文档：记录真实证据如何推动当前 Public Preview 演进到 v1.0。
> 新用户请先阅读 [中文 README](README.zh-CN.md)。本文不是第二本使用手册、日常开发日志，也不要求开发者手工统计指标。

## 一、演进原则

RigorBreeze 面向一个人长期使用 Codex 交付真实项目。演进遵循四条规则：

1. 真实问题先于新增能力；
2. 能通过项目配置解决的问题，不修改通用核心；
3. 普通问题重复出现两次再改，高风险放行缺陷发生一次就修；
4. 先写失败回归，再修改 Skill，并用真实切片重新验证。

版本号不按命令、字段或文档数量升级。v1.0 表示经过真实项目验证，而不是功能看起来足够多。

## 二、日常使用

在项目中调用：

```text
$rigorbreeze 开发这个需求
```

Codex 负责恢复状态、维护一份任务合同、执行 RED、实现、验证、证据和归档门禁。开发者只在三个时点参与：

1. 确认目标、范围、原型和验收标准；
2. 验收真实页面、接口、真机或运行结果；
3. 非 L0 切片结束时确认三项复盘判断。

最短流程：

```text
确认一个用户结果
→ 冻结任务合同
→ L1/L2/Emergency 观察 RED
→ 小步实现并运行 affected
→ L1/L2 运行 enforced full
→ 真实验收和独立审查
→ 自动复盘摘要与三项确认
→ archive
```

`archive` 只表示任务完成。不可变制品、灰度、SLO、告警和回滚证据仅在明确发布时进入门禁。

当同一项目要由多个窗口同时写代码时，Codex 自动为每个任务创建
`rigorbreeze/<task-id>` 分支和独立 worktree，并用 `status --all --json`
汇总。普通任务仍不创建 DAG；复杂需求由 Codex 先展示一次精简依赖
关系，用户一次确认后才创建带 `Depends-On` 的任务。

## 三、实践数据如何记录

### 自动采集，不要求人记忆

每次任务的机器事实继续写入唯一证据文件：

```text
spec/evidence/<TASK-ID>.json
```

Skill 自动记录、计算和预填：

- 规格批准摘要、Git 基线、任务与项目指纹；
- RED、测试文件摘要、检查命令、结果和耗时；
- affected/full 执行次数、失败检查和验证新鲜度；
- 失败事件的即时分类，例如静态质量、行为、安全、供应链、迁移、构建和验收；
- advisory 失败，作为潜在绕过候选；
- 第一次验收结果、验收记录数；
- 从失败到后续成功的返工时间估算；
- 代码变化引起的证据失效；
- 报告、截图、设备、Git SHA、制品及其摘要；
- 当前阶段，以及由状态确定性推导的 `nextAction`。

这里不再建立第二套“实践事件日志”。事件发生时直接在原有检查记录中分类，归档前再从同一证据文件生成复盘摘要。

### 人只判断三件事

L1、L2 和 Emergency 归档前，Codex 展示自动生成的 `retro --json` 摘要。开发者只需确认：

1. **返工原因**：无返工，或主要属于需求、设计、实现、测试、环境、流程或混合原因；
2. **异常是否合理**：是否出现误阻断、错误 `nextAction` 或不合理绕过；没有就回答“无”；
3. **流程是否有帮助**：helped、neutral 或 hurt。

预填摘要和确认都写回同一个 evidence JSON，并绑定当前任务摘要和项目指纹。摘要变化后旧确认自动失效。

### 归档保护

- L0 不强制复盘，避免给低风险任务增加成本；
- L1/L2/Emergency 在归档前自动生成摘要；
- 未确认三项判断时，`archive` 明确提醒并阻断；
- 流程返工、异常阻断/`nextAction` 或 `hurt` 会自动写入 `evolutionCandidate: true` 并提示；
- Codex 负责运行命令和预填信息，不把 CLI 操作转交给用户；
- Git 自动化默认 `manual`。项目显式选择 commit、push、merge 或 release
  后，Codex只在该级别和全部门禁范围内执行；生产迁移与回滚仍需独立授权。

### 真实开发如何推动 Skill 演进

你正常开发即可，不需要额外维护日志：

```text
任务结束自动复盘
→ 你只确认三项判断
→ 负向信号自动标记为演进候选
→ Codex 当场提示候选已记录
→ 普通同类问题第二次出现，或高风险放行问题第一次出现
→ 调用 $rigorbreeze 汇总这个项目的演进候选
→ Codex 读取 spec/evidence/*.json 并分类
→ 核心问题先写失败回归，再最小修改 Skill
→ 下一个真实切片验证改进
```

项目证据默认只保存在项目内，不自动上传，也不会让 Skill 自行改写自己。跨项目演进时，由你把相关项目路径交给 Codex；Codex负责读取和汇总，不要求你手工复制指标。

## 四、什么时候修改 Skill

自动摘要发现异常后，先分类：

| 分类 | 默认处理 |
|---|---|
| Skill 核心错误 | 写回归测试后修核心 |
| 项目配置错误 | 修改 `rigorbreeze.toml` |
| 技术栈差异 | 修改或新增可选适配器 |
| 外部环境故障 | 修环境，保留 Blocked 事实 |
| 使用方式问题 | 优先改善提示，不急着加门禁 |
| 门禁正确发现风险 | 不修改 Skill |

普通改进进入核心前应满足：

- 同类问题在真实切片至少发生两次；
- 能稳定复现并说明实际损失；
- 已排除配置、工具和环境问题；
- 能用自动化回归保护；
- 修改后不会让所有普通任务承担额外固定成本。

以下问题发生一次即可修复：

- 旧证据被错误当作当前证据；
- 秘密、越权、破坏性迁移或错误制品被门禁放过；
- 状态升级丢失历史；
- 必要条件缺失时 release 错误通过；
- `status`/`doctor` 修改事实；
- Skill 自动执行未经授权的 Git 或生产动作。

删除或放宽门禁时，必须有误阻断证据和更窄的替代规则，不能因为一次执行慢就直接绕过。

## 五、从实践到下一个版本

安全的演进闭环是：

```text
自动采集与即时分类
→ archive 前生成复盘摘要
→ 人确认三项判断
→ 跨切片聚合同类问题
→ 判断核心、配置或适配器责任
→ 先写失败回归
→ 做最小修改
→ 在下一个真实切片验证
```

Skill 不会在业务项目中自行改写源码或自动宣布升级。自动化负责采集、计算、预填和提出候选；修改通用规则仍需要可审计的测试和人的最终决定。

若需要保存一次跨切片决策，只记录以下最小内容，不复制原始日志：

```markdown
### EV-001
- 证据任务：
- 重复问题：
- 分类：核心 / 配置 / 适配器 / 环境 / 非问题
- 实际影响：
- 决定：观察 / 修复 / 项目适配 / 拒绝
- 回归测试：
- 下一切片验证结果：
```

### 首次真实切片：OOM 治理任务（v0.5.2）

正在执行的高风险治理任务第一次把 RigorBreeze 放进真实、跨仓库、会中途扩张的开发过程。它暴露的不是“再加一套流程”，而是已有合同中六个可能错误放行或延迟发现的缺口：

- 重新批准可能把已经修改的生产代码洗入新的 RED 基线；
- RED 可以引用合同中不存在的验收 ID，且自然语言 Allowed Scope 无法稳定机器检查；
- 范围外修改直到提交阶段才暴露，已提交变化也可能被忽略；
- 多条 RED 中可能存在未由当前 full 验证闭合的链；
- L2 修改依赖或迁移时，项目 profile 可能漏掉供应链或迁移检查；
- 执行器导入辅助模块时可能在业务项目生成 `__pycache__`。

v0.5.2 已通过失败回归和最小核心修改收口这些问题，同时补充三条边界：不能在生产变化之上重新批准；回退生产变化后可以修订同一可观察结果，新增用户结果或验收条件才切依赖任务；跨仓库任务各自保存合同和证据；临时或合成凭证只能证明构建，不能作为真实验收或发布证据。

后续代码级复核又发现并修正了四个范围边界：单层 `*` 不再跨目录，`**` 保留递归语义；包含空格和特殊字符的 Git 路径使用 NUL 分隔解析；策略、配置和 runner 不再默认绕过 Allowed Scope；已提交文件类型变化进入完整任务变化集。这些属于现有承诺的正确性收口，不增加公共能力。

本轮明确没有新增跨仓库全局注册表、任务规模自动评分、第二套实践日志或新的公共命令。以下候选留给下一次真实切片验证，不因一次观察继续增加代码：

1. “同一结果先回退再修订、新结果切依赖任务”的分类是否减少返工和任务碎片；
2. `scope` 的 `nextAction` 能否准确指出应回退、修正还是拆分，是否存在误阻断；
3. L2 动态检查能否在真实依赖/迁移任务中发现遗漏，同时避免要求无关企业工具；
4. 跨仓库 Authoritative inputs 是否足够恢复上下文，只有重复不足时才考虑机器关联；
5. doctor 的基线提示是否及时且不打扰首次使用；
6. 合成凭证边界能否阻止“构建成功”被误报为真实环境验收。

### 第二次真实摩擦：交付动作与内核集中度（v0.6.0）

真实任务完成验证并由用户明确要求提交、推送时，`manual` 的旧解释仍迫使 Codex 在“完全不执行”与“绕开 RigorBreeze 手工执行”之间选择。这个摩擦说明缺失的是一次明确授权的安全通道，而不是默认提高所有项目的自动化权限。同时，持续安全收口使 `flow.py` 集中到约 3094 行，状态升级、门禁和 Git 交付修改容易共享同一冲突面。

v0.6.0 将用户意图和长期授权分开：`manual` 继续不授予无人值守权限，但用户当前消息可以单次授权 commit 或 push；该权限不写入配置、不跨任务或 Session，也不覆盖 merge、release、生产迁移或回滚。push 必须绑定准确 remote、当前分支和 expected HEAD，先 fetch，只允许 fast-forward，禁止 rebase/force，并核对远端 SHA。直推集成分支额外要求当前 full 验证、结构化验收和审查。

内核同时按真实职责拆成 CLI 编排、state、policy、parallel 和 automation，保持单一项目入口、schema v3、automation journal v1 和最小 Spec Tree。拆分的目的不是追求文件数量，而是让状态升级不误碰 Git 自动化、门禁可独立测试，并把后续问题定位到稳定职责边界。

自动 commit 后通过 Git 私有日志中的 parent/tree 与任务、evidence、验证、项目指纹摘要桥接原验证，避免提交本身使验证立即过期；日志仍不成为第二套质量证据，也不改写 tracked evidence。

| 维度 | v0.5.2 | v0.6.0 评估 |
|---|---:|---:|
| 内核可维护性 | 86 | 92 |
| Git 自动化安全 | 92 | 94 |
| Git 交付体验 | 84 | 93 |
| 单人开发适配 | 95 | 96 |
| 静态工程就绪度 | 93 | 94 |
| 真实项目成熟度 | 80 | 82～84 |
| 综合成熟度 | 89 | 89～90 |

以上评分是对当前个人 Codex 生产开发场景的工程判断，不代表 Star、社区规模或客观排名。综合成熟度仍不能仅靠重构宣称超过 90，必须继续由真实 L1/L2、并行 DAG、远程 CI 和受保护交付演练证明。

| 项目 | 场景化项目成熟度 | 当前个人场景适配 |
|---|---:|---:|
| RigorBreeze v0.6.1 | 89～90 | 96 |
| [Superpowers](https://github.com/obra/superpowers) | 95 | 89 |
| [mattpocock/skills](https://github.com/mattpocock/skills) | 92 | 88 |
| [GitHub Spec Kit](https://github.com/github/spec-kit) | 93 | 82 |
| [OpenSpec](https://github.com/Fission-AI/OpenSpec) | 91 | 85 |
| [Wu5 Dev Flow](https://github.com/WenOwen/wu5-dev-flow) | 77 | 82 |

### 第三次真实摩擦：Worktree 生命周期（v0.6.1）

`home-api` 的媒体路由任务留下了一个干净的额外 worktree 和本地任务分支。该目录约占 69 MB，任务提交已经通过补丁等价的 cherry-pick 进入 `main`，但不是 `main` 的祖先；同时旧任务没有保留 RigorBreeze 创建来源。原实现因此既无法把它识别为集成，也不会在 `status --all` 中发现这个未登记 worktree。

v0.6.1 只补两个小闭环：当已记录基线之后的全部任务提交都能在基准分支找到补丁等价结果时认定已集成，任何部分等价仍保持活动；`status --all --json` 增加 `cleanup` 投影，区分可清理、带原因保留和未登记 worktree，并显示按策略保留的本地任务分支。Codex 在看到可清理项后主动运行现有 `reconcile --cleanup`，不要求开发者记住周期性维护。

本轮没有增加自动删除分支、远程分支清理、后台定时器、来源猜测或 `--force`。只有真实项目反复出现大量已集成本地分支时，才重新评估一个必须显式请求且只处理已证明集成分支的可选能力。

| Worktree 生命周期维度 | v0.6.0 | v0.6.1 |
|---|---:|---:|
| 集成方式兼容 | 76 | 92 |
| 清理发现能力 | 82 | 94 |
| 清理安全 | 94 | 95 |
| 防止长期堆积 | 85 | 93 |
| 综合生命周期闭环 | 87 | 92 |

### 第四次真实摩擦：工作流激活、共享运行资源与发布恢复（v0.7.0）

后续真实多窗口开发证明，文件隔离本身不等于流程已生效，也不能隔离端口、watcher、本地网关、微信开发者工具或发布环境。另一个高风险发布还暴露出两类问题：功能关闭与依赖不可用模式没有在服务器前被强制验证；“迁移和部署”这一概括掩盖了多个有顺序、停止点和不可重复动作的步骤。

v0.7.0 因此只收口四个已观察缺口：Skill 必须先用 bundled runner 读取机器状态，活动任务期间不覆盖旧项目执行器；废弃任务通过带原因的 `abandoned` 关闭释放任务槽但保留 Git 恢复目标；`Runtime-Claims` 在跨 worktree 阻断共享运行资源；L2 通过 `Operational-Modes` 和机器 `operation-plan`/`operation-result` 证明配置矩阵并给出唯一安全恢复入口。state/evidence schema 升至 v4，历史记录原样保留。

这些事件由 runner 自动去重写入原任务 `practice.events`。正确阻断只用于统计，不自动形成演进候选；用户仍只判断误阻断或 nextAction 是否合理、返工原因、流程最终 helped/neutral/hurt。RigorBreeze 不读取聊天全文，也不自动修改已安装 Skill。

| 维度 | v0.6.1 | v0.7.0 评估 |
|---|---:|---:|
| 工作流真实激活 | 82 | 94 |
| 并行运行资源安全 | 75 | 93 |
| 任务终止与恢复 | 82 | 93 |
| L2 配置与发布恢复 | 84 | 94 |
| 单人开发适配 | 96 | 96 |
| 静态工程就绪度 | 94 | 95 |
| 真实项目成熟度 | 82～84 | 85～87 |
| 综合成熟度 | 89～90 | 90（仍属 Public Preview） |

下一步不继续堆核心能力，而是在真实 L1、共享资源并行和完整 L2 发布中验证：旧 runner 处理是否顺滑、资源声明是否误阻断、三模式证据是否有实际价值，以及暂停恢复是否避免重复迁移和扩大故障。

### 第五次真实摩擦：状态收尾把成本转移给后续任务（v0.8.0）

后台支付、厂商接口和并行小程序任务证明，RigorBreeze 能让高风险发布停在安全位置、保留旧实例、形成回归测试并限制范围，但项目级状态没有同步收尾：已集成任务仍被 `baselineStale` 覆盖，主 worktree 的 `spec/state.json` 成为未跟踪噪音，初次 L1/L2 缺少安全基线提交路径，archive 后又失去提交归档合同和 evidence 的活动上下文，未登记但已集成的 worktree 只能长期报告。

v0.8.0 因此只修复这条历史成本链：状态统一迁入 Git 私有目录；`workflowBaseline` 在真正 base branch 证明安装并支持一次性受控提交；`integrated-unclosed` 和 `closure-pending` 优先于基线新鲜度建议；正常 archive 保存不可变 `lastClosed` 用于后续受保护交付；`reconciled` 诚实收口外部已集成任务；未登记 worktree 只有在绝对路径、SHA、干净状态和完整集成证明全部成立且用户本次明确授权时才删除，分支始终保留。

这次没有增加 Spec 文件类型、第三方依赖、第二套状态系统或微型任务模式。L0 仍只需要最小合同、affected 和 archive。L2 文档补充脱敏真实/厂商沙箱 fixture，以及文本 JSON、序列化、URL 编码、数据库方言和订单前置条件，但核心不假装能够预言所有第三方生产行为。

| 维度 | v0.7.0 | v0.8.0 评估 |
|---|---:|---:|
| 工作流基线可落地性 | 78 | 94 |
| 状态与下一动作准确性 | 84 | 95 |
| 历史任务诚实收口 | 76 | 94 |
| 归档后 Git 交付闭环 | 80 | 94 |
| worktree 长期整洁度 | 88 | 95 |
| 单人开发适配 | 96 | 97 |
| 静态工程就绪度 | 95 | 96 |
| 真实项目成熟度 | 85～87 | 88～90 |
| 综合成熟度 | 90 | 91（仍属 Public Preview） |

下一轮只在 v0.8.0 完成三仓基线、历史收口和清理 dogfood 后评估 Agent 行为可靠性，候选版本为 v0.9.0。本轮不建立 `evals/`，也不把正确阻断自动升级为核心改动。

## 六、v1.0 的真实验证条件

至少完成：

- 一个普通 L1 垂直切片；
- 一个包含权限、迁移或发布治理的 L2/高风险切片；
- 两个独立任务的一次真实并行 worktree 开发；
- 至少三个节点的一次真实依赖 DAG 开发；
- 一次适用的灰度、监控和回滚演练。

随后必须用证据回答：

1. L1 能否从 draft 顺畅运行到 archive；
2. 高风险切片能否完成 full、真实验收和适用治理；
3. 新 Session 中 `status --json` 的 `nextAction` 是否正确；
4. affected 是否漏检或运行过多；
5. full profile 的实际耗时是否可接受；
6. 证据录入是否仍然繁琐或重复；
7. 哪些门禁发生误阻断；
8. 是否出现未解释的人工绕过；
9. 证据失效范围是否过大或过小；
10. 灰度、监控和回滚字段是否足以完成真实演练；
11. 流程最终减少了返工和逃逸风险，还是只把时间转移到填写证据。
12. 两个窗口是否出现错认任务、共享目录写入或注册表无法恢复；
13. DAG 是否只在复杂任务出现，依赖阻断和集成后 ready 是否正确；
14. Allowed Scope 重叠是否漏报或产生不可接受的误阻断；
15. baseline 变化后的重新验证范围是否正确；
16. commit/push 是否在真实任务分支稳定后才逐级启用，且没有包含无关文件；
17. provider auto-merge 是否真正受 Required Checks 保护；
18. release 中断重试是否避免重复发布同一制品。

如果关键问题仍是 Fail，继续发布 v0.x 并修复真实问题。全部得到可信结论、重复缺陷已修复且流程成本可接受后，才由人确认 v1.0。

## 七、明确不进入当前路线

当前不把以下团队化或高维护设计加入单人核心：

- 多 Agent 团队排期、人员分配、远程接管或 Agent 控制台；
- 多个写窗口直接共享同一物理工作目录；
- 多套互不兼容的状态系统；
- 为普通独立任务默认创建 DAG；
- 内置 Issue Tracker、看板和协作平台；
- 强制知识图谱、向量数据库或大型索引；
- 多层重复 Spec 文档树；
- 所有项目必须声明全部标准检查或填写大量 N/A；
- 重复的 evidence、attestation 和手工实践日志；
- 未经长期配置或用户当前消息单次明确授权的 commit/push；未经长期配置的 merge、tag、release 或生产操作；
- AI 自动接受视觉、安全、法务或生产结论；
- 把普通任务归档绑定到制品、灰度、SLO 和生产发布；
- 为一次性问题建立永久抽象；
- 为了收集指标增加新的固定问答。

代码图谱、Figma、微信开发者工具、云平台发布、并发检查等能力，只有真实项目证明有必要时才作为可选适配器加入，不成为所有用户的固定成本。

## 八、工程整理状态与未来方向

| 整理项 | 当前状态 | 后续规则 |
|---|---|---|
| 合并重复测试 fixture | 已于 v0.2 完成 | 保持共享fixture，不为减少行数建立复杂测试框架 |
| 按职责拆分核心脚本 | 已于 v0.6.0 收口 | CLI、state、policy、parallel、automation 单向依赖，入口与证据模型保持统一；不为追求行数继续机械拆分 |

核心脚本拆分属于结构重构，不是能力升级。后续不得仅因文件超过某个
行数继续拆分；只有职责边界和重复维护成本真实出现时才调整，并保持
CLI、schema、证据和门禁行为不变，先运行完整回归。

v0.5.1 后，自动创建的 worktree 才带有可删除来源；手工或注册表重建
得到的 worktree 只保留并报告。commit、push、merge 和 release 的结果
写入 Git 私有 `automation.json`，不在动作完成后再次修改 tracked
evidence。该日志只负责外部动作恢复和幂等，不是第二套需求或质量证据。
v0.6.0 起，新日志还区分长期授权与 `user-once`；单次授权仅响应用户当前消息，不改变项目等级。

并行和自动交付能力按真实实践逐级稳定：

```text
先完成真实并行切片与三节点 DAG
→ 汇总冲突、等待、重跑、误阻断和节省时间
→ 稳定后把 manual 提升到 commit
→ commit 在真实切片稳定后提升到 push
→ 受保护环境分别演练 provider auto-merge 和 release
→ 才判断这些能力是否达到 v1.0
```

任何等级的失败或摩擦都进入原有任务 evidence 与复盘候选。提示仍为：

```text
$rigorbreeze 汇总这个项目的演进候选
```
