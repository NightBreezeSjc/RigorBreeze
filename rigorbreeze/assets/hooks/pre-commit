#!/bin/sh

# Advisory only. Remote required CI remains the merge authority.
if ! python3 scripts/rigorbreeze.py check commit; then
  echo "RigorBreeze advisory: commit gate reported risks." >&2
  echo "The local hook does not block; review the output before continuing." >&2
fi

exit 0
